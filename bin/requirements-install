#!/bin/bash


source "$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )/.base"

cd "${PROJECT_LOCATION}"
echo
echo "INSTALLING REQUIREMENTS"
echo "--------------------------------------------------------------------------------"
pip install -r requirements.txt
echo
cd "${CURRDIR}"
